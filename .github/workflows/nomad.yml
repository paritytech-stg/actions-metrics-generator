name: Nomad test

on:
  workflow_dispatch:


jobs:
  job1:
    runs-on: cattery-nomad-local
    steps:
      - run: |
          echo 'nomad runner'
          echo 'curl -L http://example.com'
          curl -L http://example.com


